# API Reference

Complete API documentation for the Quark ORM.

## Core API

- **[Client](/quark-docs/docs/reference/api/client)** - Creating and configuring the ORM client
- **[Query Builder](/quark-docs/docs/reference/api/query-builder)** - Fluent type-safe query construction
- **[CRUD](/quark-docs/docs/reference/api/crud)** - Create, Read, Update, Delete operations
- **[Querying](/quark-docs/docs/reference/api/querying)** - Retrieving data: List, Find, aggregates, pagination

## Model Definition

- **[Modeling](/quark-docs/docs/reference/api/modeling)** - Struct tags, relations, hooks

## Schema & Data

- **[Transactions](/quark-docs/docs/reference/api/transactions)** - ACID transactions and savepoints
- **[Migrations](/quark-docs/docs/reference/api/migrations)** - Schema migrations and sync

## Advanced Features

- **[Caching](/quark-docs/docs/reference/api/caching)** - Query result caching with Redis/Memory
- **[Multi-Tenant](/quark-docs/docs/reference/api/multi-tenant)** - Row-level, schema, and database isolation
- **[Observability](/quark-docs/docs/reference/api/observability)** - Middleware, OpenTelemetry, events

## Reference

- **[Dialects](/quark-docs/docs/reference/api/dialects)** - Database dialect interface
- **[Errors](/quark-docs/docs/reference/api/errors)** - Error types and handling
- **[Routines](/quark-docs/docs/reference/api/routines)** - Stored procedures and functions

## Quick Example

```go
package main

import (
    "context"
    "database/sql"
    "log"
    
    _ "github.com/lib/pq"
    "github.com/jcsvwinston/quark"
)

type User struct {
    ID    int64  `db:"id" pk:"true"`
    Name  string `db:"name"`
    Email string `db:"email" quark:"unique"`
}

func main() {
    db, _ := sql.Open("postgres", "...")
    client, _ := quark.New(db)
    ctx := context.Background()
    
    // Create
    user := &User{Name: "Alice", Email: "alice@example.com"}
    quark.For[User](ctx, client).Create(user)
    
    // Read
    u, _ := quark.For[User](ctx, client).Find(user.ID)
    
    // Query
    users, _ := quark.For[User](ctx, client).
        Where("active", "=", true).
        OrderBy("created_at", "DESC").
        List()
    
    // Update
    u.Name = "Alice Smith"
    quark.For[User](ctx, client).Update(&u)
    
    // Delete
    quark.For[User](ctx, client).Delete(&u)
}
```
