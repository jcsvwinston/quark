// Code generated by Quark CLI. DO NOT EDIT.
package {{.Package}}
{{if or .HasTimeField .HasJSONRawMessage .HasQuark}}
import (
	{{- if .HasTimeField}}
	"time"
	{{- end}}
	{{- if .HasJSONRawMessage}}
	"encoding/json"
	{{- end}}
	{{- if .HasQuark}}

	"github.com/jcsvwinston/quark"
	{{- end}}
)
{{end}}
type {{.StructName}} struct {
	{{- range .Fields}}
	{{.Name}} {{.Type}} `{{if .RelTag}}rel:"{{.RelTag}}" join:"{{.JoinTag}}"{{else}}db:"{{.JSONTag}}"{{if .IsPK}} pk:"true"{{end}}{{if .QuarkTag}} quark:"{{.QuarkTag}}"{{end}}{{if .JSONTag}} json:"{{.JSONTag}}"{{end}}{{end}}`
	{{- end}}
}

func ({{.StructName}}) TableName() string {
	return "{{.TableName}}"
}
